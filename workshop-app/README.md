# Workshop App

Minimal HTTP app used across the AKS workshop.

## Endpoints
- `/` returns runtime information as JSON.
- `/healthz` returns readiness for liveness checks.
- `/readyz` returns readiness for startup checks.
