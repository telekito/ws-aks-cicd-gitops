apiVersion: v1
kind: Service
metadata:
  name: workshop-app
  namespace: aks-workshop
spec:
  selector:
    app: workshop-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
